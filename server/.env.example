# SPDX-License-Identifier: Apache-2.0
# Copyright 2026 CLSOFTLAB (씨엘소프트랩), Dr. Lee Il-guk (이일국)
#
# Copy this file to `.env` (never commit .env) and fill in your real key.
# The key lives ONLY on the server and is NEVER exposed to the browser.

# Your Anthropic API key. Get one at https://console.anthropic.com/
# It starts with "sk-ant-". DO NOT put the real value in the repo or the browser.
ANTHROPIC_API_KEY=<your-anthropic-api-key-here>

# Model to use. Cost-first default = claude-haiku-4-5 ($1/$5 per MTok).
# Raise to claude-sonnet-5 or claude-opus-5 for higher quality (higher cost).
AI_MODEL=claude-haiku-4-5

# Effort for sonnet/opus (ignored for Haiku, which takes no thinking/effort).
AI_EFFORT=low

# Cost guardrails.
AI_RATE_LIMIT=20               # max requests per minute per IP
AI_MONTHLY_TOKEN_CAP=2000000   # monthly token budget; over budget → 429 {fallback:true}

# Port the proxy listens on.
PORT=8787

# Allowed browser origin for CORS (use your site origin in production; "*" for local dev).
CORS_ORIGIN=*
